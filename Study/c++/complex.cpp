#include <iostream>

using namespace std;
int main(){
    short month[12]; // 12 size인 배열을 선언 - 같은 데이터형의 집합(여기서는 short 형태 )
    short initial_month[12] = {1,2,3}; // 초기화
    cout << initial_month[0] << endl; 
    // 문자열 
    char a[] = "Hello";
    cout << a << endl;
    return 0;
}